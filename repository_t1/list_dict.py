# firewall lambda handler -- convert list to tuple then convert into dict first then _ initializing tuples ,  test_tup1 = ('GFG', 'is', 'best') ,  test_tup2 = (1, 2, 3)

res = {}
list_keys = [1, 2, 3, ]
list_values = ['blue', 'green', 'orange', ]


def al_ba(Folder_re_pod):
    # Folder_re_pod
    for i in list_values:
        print(i)

    return i


class Folder_re_pod:

    # print(str(list_keys))
    # print(str(list_values))
    # Using zip() + dict() ; dict() will carry tuple key-value pairs if mapped with df_values
    # Convert Tuples to Dictionary

    if len(list_keys) == len(list_values):
        res = dict(zip(list_keys, list_values))

    print(str(res))

    def co(self):
        for i in self:
            if i == "%o%":
                print(i)

        return i

    print(co(list_values))


print(al_ba(Folder_re_pod))
